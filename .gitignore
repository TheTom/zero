/target
**/*.rs.bk
.DS_Store
# session transcripts / logs are local, never committed
/sessions
*.jsonl
# planning docs + roadmap are local-only, never committed
/docs
ROADMAP.md
*.roadmap.md
# local config / discovered servers
/config.json
/servers.json
# agent instructions are local-only (like /docs)
CLAUDE.md
# python bytecode from research scripts
__pycache__/
